import React from "react";


function Sidebar(props) {

  return (
    <div className="sidebar-main">
     
    </div>
  );
}

export default Sidebar;

{
  /* {menuCategory.map((menu, index) => (
          <DropdownButton
            as={ButtonGroup}
            key={index}
            id="dropdown-button-drop-end"
            drop="end"
            variant="secondary"
            title={menu}
            style={{ margin: 10 }}
            onClick={() => handleMenuList(menu)}
          >
            {(selectedMenu === "App" ? appMenu : softwareMenu).map(
              (subMenu, index) => (
                <Dropdown.Item eventKey={index} key={index}>{subMenu}</Dropdown.Item>
              )
            )}
          </DropdownButton>
           */
}
